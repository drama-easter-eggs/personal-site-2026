---
import { careThreads } from "../../data/site";
---

<section id="care" class="section care">
  <div class="shell">
    <header class="care__head">
      <p class="eyebrow" data-reveal>02 — What I care about</p>
      <h2 class="h1 zh care__title" data-reveal style="--reveal-delay:60ms">我關心什麼</h2>
    </header>

    <div class="care__intro prose-zh" data-reveal>
      <p>這幾年我做了各種不同領域的研究，我發現，那些持續縈繞在心頭而停留的問題是：</p>
    </div>

    <!-- The two questions are the emotional centre of the page — set as display. -->
    <blockquote class="care__question" data-reveal style="--reveal-delay:80ms">
      <p class="h2 zh">
        人們為什麼會被某些東西打動？<br />
        那些感受，又如何改變人們的選擇與行動。
      </p>
    </blockquote>

    <div class="care__intro prose-zh" data-reveal>
      <p>願意相信一個產品、喜歡一個故事角色、持續使用一項服務，甚至願意投入大量時間、金錢與情感。</p>
      <p>在商業世界中，常需要在有限的時間，快速理解市場與受眾，但當我們太快把人壓縮成標籤時，那些真正影響行為的因素，反而被消失了。</p>
      <p>而我願意投入自己，深深地去理解：</p>
    </div>

    <p class="h2 zh care__thesis" data-reveal style="--reveal-delay:60ms">
      人如何從一段經驗產生感受、賦予意義，最後形成選擇與行動。
    </p>

    <ol class="care__threads">
      {careThreads.map((thread, i) => (
        <li class="care__thread" data-reveal style={`--reveal-delay:${i * 90}ms`}>
          <span class="care__thread-no">{String(i + 1).padStart(2, "0")}</span>
          <div class="care__thread-text">
            <h3 class="care__thread-title zh">{thread.title}</h3>
            <p class="care__thread-body">{thread.body}</p>
          </div>
        </li>
      ))}
    </ol>
  </div>
</section>

<style>
  .care__head {
    display: grid;
    gap: 18px;
    padding-bottom: clamp(40px, 5vw, 64px);
  }

  .care__intro { max-width: 54ch; }

  .care__question {
    margin: clamp(40px, 5vw, 64px) 0;
    padding-left: clamp(20px, 3vw, 34px);
    border-left: 1px solid var(--color-ink);
  }
  .care__question p { color: var(--color-ink); }

  .care__thesis {
    margin-top: clamp(28px, 3.5vw, 40px);
    max-width: 22ch;
    color: var(--color-ink);
  }

  .care__threads {
    list-style: none;
    margin: clamp(56px, 7vw, 88px) 0 0;
    padding: 0;
    border-top: 1px solid var(--color-rule);
  }
  .care__thread {
    display: grid;
    grid-template-columns: 64px minmax(0, 1fr);
    gap: clamp(16px, 3vw, 40px);
    align-items: start;
    padding-block: clamp(26px, 3.2vw, 38px);
    border-bottom: 1px solid var(--color-rule);
  }
  .care__thread-no {
    font-family: var(--font-display);
    font-weight: 300;
    font-size: 1.375rem;
    line-height: 1.5;
    color: var(--color-ash);
  }
  .care__thread-text {
    display: grid;
    gap: 10px;
    max-width: 58ch;
  }
  .care__thread-title {
    font-family: var(--font-sans);
    font-size: clamp(1.0625rem, 1.6vw, 1.25rem);
    font-weight: 500;
    line-height: 1.6;
    letter-spacing: 0.04em;
    color: var(--color-ink);
  }
  .care__thread-body {
    font-size: 0.9375rem;
    line-height: 1.9;
    letter-spacing: 0.04em;
    color: var(--color-ash);
  }

  @media (max-width: 640px) {
    .care__thread { grid-template-columns: 1fr; gap: 8px; }
    .care__thread-no { font-size: 1rem; }
  }
</style>
